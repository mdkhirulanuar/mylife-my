# Prototype 0.5 Change Record

Baseline: Prototype 0.4 validated main branch.

Change package: Monthly Budget + Budget vs Actual.

Files introduced: `budget.js`, `styles-v05.css`, budget automated tests and supporting documentation. `index.html` is updated only to identify Prototype 0.5 and load the budget module after the validated 0.4 application module.

The existing Prototype 0.4 calculation implementation in `app.js` is intentionally unchanged.